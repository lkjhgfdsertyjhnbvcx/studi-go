# 260807 変更まとめ（デプロイ前チェック用）

デプロイ: `vercel --prod`（このリポジトリの最終コミットは 2026-08-01。8/4以降の本番稼働コードが未コミットのため、git push ではなく従来どおり vercel CLI で。）

## 型チェック
`npx tsc --noEmit` → 本体 `src/` のエラー **0件**
（`Studi-Go (1)/outreach-tool` 配下のエラーは以前からのもので今回とは無関係）
※ `next.config.js` に `typescript.ignoreBuildErrors: true` があるため、ビルドは型エラーを素通りさせます。tsc は手動で通す必要があります。

## デプロイ後に見てほしい画面（優先順）

1. **`/admin/invites` で T.I.G Sounds を承認**
   → 承認直後に緑のパネルが出て、予約ページURL・ログインID・仮パスワードが表示されること
   → 石栗様に「予約ページを公開しました／管理画面のログイン情報」メールが届くこと
   → そのID・仮パスワードで `/store/login` に入れること
2. **`/studio/{studioId}`** に予約枠が出ること（営業時間の解釈が変わったため）
3. **`/store/dashboard`** — スタッフ管理・ブラックリスト・CSVインポート・Stripe接続が今までどおり動くこと
   （これらのAPIに認証チェックを追加したため、ここが一番のリグレッション候補）
4. **`/studigo_apply.html`** からテスト申込 → 完了文言が「今すぐお送りしました」に変わっていること
5. **`lp-switch.html` の「無料で乗り換える」** → 申込 → 運営通知に「有料プラン2ヶ月無料の対象」と出ること

## 主な変更

### オンボーディング（追伸メール不要化）
- `api/admin/invites/[token]/approve` 全面改修
  - `stripUndefined()`：Admin SDK は `ignoreUndefinedProperties` 未設定のため、
    背景画像を設定しなかった店舗の承認が**必ず500で落ちていた**
  - `staff[]` を仮パスワード付きで自動生成（無いと誰も `/store/login` に入れない）
  - `planKey: "free"` を必ずセット（無いと店舗の初回保存で `isPublished` が false に戻る）
  - 承認完了メールを自動送信。失敗時は運営通知＋管理画面に仮パスワードを表示し、
    `storeIntakes.issuedTempPassword` にも控えを残す
- `api/intake/[token]` POST：提出時に運営へ通知メール（ルーム0件・料金0円等を警告付き）
- `api/applications`：申込情報（連絡先・希望プラン・流入元LP）を招待に引き継ぎ

### バグ修正
- `lib/business-hours.ts`（新規）：営業時間パーサを共通化
  「10:00〜翌2:00」で**予約枠が0件**、「24時間営業」が無言で10-22時に化ける問題
  ※ 0時以降の枠は 24:00 で打ち切り。`startTime` が24以上だと
    `new Date(date+"T"+startTime)` が Invalid Date になり別の壊れ方をするため
- `lib/intake.ts`：`advanceDays/Hours` → `reservationWindowType/Value` に変換
  （キー不一致で**受付開始の制限が全く効いていなかった**。0＝制限なし、既定値も0に変更）
- `studio/[id]`：個人練習の `pricePerHour` を料金計算に反映（設定しても通常料金だった）
- `booking/[storeId]`：壊れていた旧予約ページを `/studio/[id]` へ恒久リダイレクト
- `lib/intake.ts`：`validateIntakeForPublish()` で部屋0件・料金0円の公開を提出/承認の両方で拒否

### セキュリティ
- `api/store/detail`：**無認証で `staff[].password`（ハッシュ）を返していた**。オーナー判定で出し分け
- `api/store/connect`：**無認証で他店舗名義の Stripe Connect オンボーディングリンクを発行できた**
- `api/staff`：**パスワードを平文保存していた**（`hashPassword` へ）＋セッション認証を追加
- `api/store/{subscribe,bookings-import,customers-import,profile}`：認証を追加
- `api/store/subscribe`：`trialDays` をサーバーの `studios.planTrialDays` から取る（任意に伸ばせた）
- `api/store/logout`（新規）：`document.cookie` では httpOnly の `__session` を消せず、
  ログアウトしてもクッキーが30日残っていた
- `api/store/register`：自己登録を410で封鎖（重複店舗の原因）

### UX
- `onboard/[token]`：営業時間の解釈をその場で表示、未保存警告、60秒後の自動保存、
  保存メッセージを保存ボタンの近くにも表示、提出エラー時も画面上部へスクロール
- `store/login`：「新規登録はこちら」→ 申込フォームへ
- `studigo_apply.html`：完了文言を実挙動に修正。mailtoフォールバック時は完了扱いにしない
- `lp-start / lp-switch`：`?lp=` を渡して流入元を計測、乗り換え2ヶ月無料を自動適用

## 残課題（未着手）
- 個人練習の単価・人数がクライアント計算のみで、`booking-onsite` にサーバー検証が無い
- `api/store/detail` の機密除去がブラックリスト方式（email/planKey/campaign 等は公開のまま）
- `api/applications` が CORS `*` かつレート制限なし
- 同一メールでの重複申込 → 重複 studios（`store/login` が空の店舗に入り得る）
- 0時以降の予約枠は未対応（`startTime` のデータモデル変更が必要）
