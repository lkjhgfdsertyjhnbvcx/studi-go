# Studi-Go デプロイ チェックリスト

型チェック（`npx tsc --noEmit`）や自動テストでは**捕まえられない**種類の不具合を、本番到達前に止めるための手順。
特にインフラ設定（Firestore/Storageルール、環境変数）を変えたときは必ず全項目を実施する。

---

## デプロイ前

- [ ] `npx tsc --noEmit` でエラーが無い（`outreach-tool` 配下の既存エラーは無視可）
- [ ] インフラ設定を変えた場合、影響範囲を grep で洗い出したか
  - Firestoreルールを締める → `firebase/firestore` を使う箇所を全部確認
  - Storageルールを締める → `firebase/storage` / `uploadImageToStorage` / アップロード系を全部確認
  - 環境変数を消す/変える → その変数を参照する箇所を全部確認
- [ ] コミット & push（`cd ~/Studi-Go_claude 2 && git add -A && git commit -m "..." && git push`）

## プレビューで確認（本番ドメインを触る前に）

Vercel の Deployments から、今回のpushのプレビューURL（`studi-go-git-main-...vercel.app`）を開いて以下を実施。

- [ ] トップページ・スタジオ一覧が表示される
- [ ] スタジオ詳細ページで空き状況カレンダーが表示される
- [ ] LINEログインできる
- [ ] 予約→決済（テストモード）→ 完了ページまで通る
- [ ] 店舗ログインできる（/store/login）
- [ ] 店舗ダッシュボード: 予約カレンダー・顧客一覧が表示される
- [ ] **店舗設定でスタジオ写真をアップロードできる** ← 今回壊れた箇所
- [ ] 管理画面ログイン（/admin/login）→ ダッシュボード表示

## 本番反映後

- [ ] Firebaseルールを変えた場合のみ `firebase deploy --only firestore:rules,storage --project studi-go-488d1`
  - ※コードのデプロイ完了後に実行すること（先にルールを締めると旧コードが壊れる）
- [ ] 本番ドメイン（studi-go.com）で上記プレビュー項目のうち主要なものを再確認
- [ ] Vercel の Logs でサーバーエラーが出ていないか確認

---

## 事故が起きたときの初動

1. Vercel → Deployments → 直前の正常なデプロイの「…」→ **Redeploy** で即ロールバックできる
2. 落ち着いてからログ（Vercel Logs / ブラウザのコンソール）で原因を特定
3. 原因が設定変更なら、その設定だけ元に戻して復旧 → 後日きちんと直す

## 補足

- 型チェックが通っても「設定・権限・外部サービス」由来の不具合は残る。手で触る確認が最後の砦。
- 将来的には Firebase を本番用・テスト用の2プロジェクトに分けると、プレビューで安全に検証できる（大きめの作業なので優先度は中）。
