rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // ⚠️ 開発用。後でユーザー認証に基づいて制限することを推奨します。
    }
  }
}
